// Root layout — thin shell only.
// All content, fonts, and providers live in app/[locale]/layout.tsx.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
